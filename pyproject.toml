[tool.poetry]
name = "data quality test"
version = "0.1.0"
description = "a data quality testing script"
authors = ["Jacobo Ceballos"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
